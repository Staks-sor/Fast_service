name: Start deploy

on:
    push:
        branches: [master]

jobs:
    deploy:
          name: Deploy to server
          runs-on: ubuntu-latest

          steps:
              - name: Checkout repository
